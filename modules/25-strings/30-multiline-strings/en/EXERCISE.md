Using a text block `"""..."""`, print text of two lines on the screen:

```text
Summer
has come
```

Adjust the indentation inside the block so that the words start from the first character, with no extra spaces on the left.
