Construye un saludo a partir de una plantilla. Sustituye la palabra `"World"` en la plantilla `"Hello, %s!"` con el método `String.format()` y muestra el resultado en la pantalla.

Salida esperada:

```text
Hello, World!
```
