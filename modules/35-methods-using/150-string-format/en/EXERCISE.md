Build a greeting from a template. Substitute the word `"World"` into the template `"Hello, %s!"` using the `String.format()` method and print the result on the screen.

Expected output:

```text
Hello, World!
```
