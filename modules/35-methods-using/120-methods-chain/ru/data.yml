---
name: Цепочки вызовов методов
definitions:
  - name: Цепочка методов
    description: >-
      прием, при котором методы вызывают один за другим, и каждый следующий
      применяется к результату предыдущего. Например,
      `"  Hexlet  ".trim().toLowerCase()`.
