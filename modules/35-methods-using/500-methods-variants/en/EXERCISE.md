
The variable `emoji` contains a sad text emoticon *-(*. Your task is to make this emoticon happy using two transformations:

  * Add eyes on the left *:*
  * Replace *(* with *)* (using the string method `replace()`)

You should get: *:-)*. Print it to the screen.
