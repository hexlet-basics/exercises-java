public class App {
    // BEGIN
    public static boolean isLannisterSoldier(String color, String shield) {
        return ("red".equals(color) && "".equals(shield)) || ("lion".equals(shield));
    }
    // END
}
