---
name: Estructura Switch
tips: []
definitions:
  - name: switch
    description: >-
      forma de elegir el comportamiento según el valor de una variable mediante
      ramas `case`, por ejemplo, `switch (count) { case 1: ... }`.
